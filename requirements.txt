# =============================================================================
# RAGL — Python Dependencies (A0 Reference Implementation)
# =============================================================================
# Synchronized with environment.yml.
# Use this file for pip-only installs:  pip install -r requirements.txt
# Prefer the conda environment for full setup (includes CUDA PyTorch + FAISS GPU).
# =============================================================================

# --- Document Processing ---
pymupdf4llm>=0.0.17
pymupdf>=1.24.0

# --- Embeddings (GPU-accelerated ONNX) ---
fastembed-gpu>=0.3.0
# Note: faiss-gpu is installed via Conda for optimal CUDA support.

# --- LLM Backend ---
ollama>=0.3.0

# --- Objective Evaluation (NLI + Cross-Encoder) ---
sentence-transformers>=3.0.0
torch>=2.0.0
nltk>=3.8.1

# --- Core ---
numpy>=1.24.0,<2.0.0
scipy>=1.11.0
tqdm>=4.66.0
rich>=13.7.0
structlog>=24.0.0

# --- Plotting ---
matplotlib>=3.8.0

# --- Testing ---
pytest>=8.0.0
pytest-cov>=4.1.0
